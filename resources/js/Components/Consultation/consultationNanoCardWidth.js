/**
 * Consultation / options-modal meal card width.
 * Fixed 240px everywhere — selected slots, sides, See other options, MealCard.
 * Do not change per screen.
 *
 * Literal class strings so Tailwind always emits them.
 */
export const MEAL_CARD_WIDTH_PX = 240;

export const MEAL_CARD_WIDTH_CLASS = 'w-[240px] max-w-[240px]';

export const MEAL_CARD_WIDTH_ONLY_CLASS = 'w-[240px]';

/** Outer article shell — fixed 240px. */
export const MEAL_CARD_DECK_SHELL_CLASS =
    'mx-auto w-[240px] max-w-[240px] shrink-0 rounded-[12px] flex flex-col bg-[#FFFFFF]';
