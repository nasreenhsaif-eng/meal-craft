/**
 * Storybook entry point: re-exports the same inner dashboard markup as the live Inertia page
 * (`AdminDashboardInner` from `Dashboard.jsx`) so titles and Tailwind stay in one place.
 */
export { AdminDashboardInner as default } from './Dashboard.jsx';
