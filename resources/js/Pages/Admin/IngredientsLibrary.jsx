/**
 * Inertia page name: `Admin/IngredientsLibrary` (IngredientLibraryController::index).
 * Thin re-export only — all UI lives in `../IngredientsLibrary/IngredientsLibraryPage.jsx`, same source Storybook uses.
 */
export { default } from '../IngredientsLibrary/IngredientsLibraryPage.jsx';
